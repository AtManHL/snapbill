<view class="ec-container">
  <canvas
    type="2d"
    id="{{canvasId}}"
    class="ec-canvas"
    canvas-id="{{canvasId}}"
    bindinit="init"
    bindtouchstart="{{touchStart}}"
    bindtouchmove="{{touchMove}}"
    bindtouchend="{{touchEnd}}"
  />
</view>
